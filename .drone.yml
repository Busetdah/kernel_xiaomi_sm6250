--- 
clone: 
  depth: 1
kind: pipeline
name: Miatoll
steps: 
  - 
    environment: 
      TG_TOKEN: 
        from_secret: TOKEN
      CHANNEL_ID:
        from_secret: CHATID
    commands: 
      - "pacman -Sy cpio && bash build_script.sh"
    image: azrim89/kernel:latest
    name: Build
